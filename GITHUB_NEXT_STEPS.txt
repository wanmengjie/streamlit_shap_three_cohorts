================================================================================
GitHub 推送（需在本机完成，需你登录 GitHub）
================================================================================

当前状态
--------
- 本目录已是独立仓库（.git 在此文件夹内），分支：main
- 最近一次提交示例：Add Streamlit DCC app for deployment
- 已跟踪运行 Streamlit 所需的核心代码：streamlit_shap_three_cohorts.py、config.py、
  requirements.txt、.gitignore、data/、utils/

冠军模型（已改 .gitignore：仅允许 3 个 joblib 进仓库）
------------------------------------------------------------------
本地须先存在（跑过各队列 prediction 步骤后）：
  Cohort_A_Healthy_Prospective/01_prediction/champion_model.joblib
  Cohort_B_Depression_to_Comorbidity/01_prediction/champion_model.joblib
  Cohort_C_Cognition_to_Comorbidity/01_prediction/champion_model.joblib

提交并推送：
  git add .gitignore
  git add Cohort_A_Healthy_Prospective/01_prediction/champion_model.joblib
  git add Cohort_B_Depression_to_Comorbidity/01_prediction/champion_model.joblib
  git add Cohort_C_Cognition_to_Comorbidity/01_prediction/champion_model.joblib
  git commit -m "Add champion_model.joblib for Streamlit Cloud"
  git push

若单个 joblib >100MB：用 Git LFS（git lfs track "*.joblib"）后再 add/commit。

分析数据（load_df_for_analysis）
------------------------------------------------------------------
仍可能缺 imputation_npj_results/.../step1_imputed_full.csv；若该 CSV >100MB 勿直接 push，
需缩小样本、换路径或启动时从网盘下载。

--------------------------------------------------------------------
第一步：在 github.com 新建空仓库（不要初始化 README 也可）
--------------------------------------------------------------------

第二步：在本目录 PowerShell 执行（把 URL 换成你的真实仓库地址）

  cd "C:\Users\lenovo\Desktop\因果机器学习"
  git remote add origin https://github.com/你的GitHub用户名/你的仓库名.git
  git push -u origin main

若提示登录：用 GitHub 网页生成的 Personal Access Token 代替密码，
或使用 GitHub Desktop / gh auth login。

--------------------------------------------------------------------
第三步：Streamlit Cloud
--------------------------------------------------------------------
- Main file path: streamlit_shap_three_cohorts.py
- Requirements: requirements.txt

================================================================================
